# Server Configuration
HOST=0.0.0.0
PORT=3000

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=library_return
DB_USER=postgres
DB_PASSWORD=postgres

# Database SSL Configuration
DB_SSL_MODE=require
DB_SSL_ROOT_CERT=./docker/postgres-ssl-certs/ca-cert.pem

# JWT Configuration
JWT_SECRET_KEY=
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=1440

# MQTT Configuration
MQTT_BROKER=localhost
MQTT_PORT=8883
MQTT_USERNAME=mqtt_user
MQTT_PASSWORD=your_mqtt_password

# MQTT TLS Configuration
MQTT_USE_TLS=true
MQTT_TLS_INSECURE=false
MQTT_CA_CERT=./certs/mqtt-ca-cert.pem